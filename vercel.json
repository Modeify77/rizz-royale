{
  "buildCommand": "npm run build -w @rizz/shared && npm run build -w @rizz/client",
  "outputDirectory": "packages/client/dist",
  "framework": "vite",
  "rewrites": [
    { "source": "/(.*)", "destination": "/" }
  ]
}
